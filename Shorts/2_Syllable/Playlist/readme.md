# Playlist Tagging & Channel Structure

Playlist organisation for the PRONUNCED YouTube channel — how each pronunciation Short is sorted into playlists, plus the channel's overall structure.

## Files

| File | What it is |
|---|---|
| `playlist_context.md` | The tagging rulebook: channel identity, full playlist roster, and the rules for sorting any word into the right playlists. Paste it at the start of a tagging request for consistent results. |
| `Playlist_-_31st_May_UPDATED_v2.xlsx` | Per-video tagging — every word mapped to a primary playlist + up to two secondaries, with rationale notes. |
| `New_Playlists_Generated.xlsx` | Proposed new Type playlists (Nut, Chili Pepper, Tea & Infusion, Mushroom, Dumpling, Cured Meat) with descriptions and word lists. |
| `Pronunciation-Channel-Blueprint_UPDATED.xlsx` | Full channel architecture: ~66 playlists across 10 sections, build phases, descriptions, and the home-tab layout plan. |

## Workflow

1. Collect a raw list of candidate words (one per row).
2. Paste `playlist_context.md`, then the word list: *"Organise these into the channel playlists using playlist_context.md."*
3. Optionally attach Semrush / keyword-volume data for priority scoring.
4. Output: a tagged table (primary + secondary playlists per word), flagged items, and any new-playlist candidates.

The rulebook handles single-country cuisines, shared dishes across countries, taxonomy checks (e.g. ube is a yam, not a fruit), and keeping difficulty playlists a layer rather than a dumping ground.

## Notes

- `.xlsx` files are binary — GitHub won't show line-by-line diffs; each commit replaces the file.
- Public repo: don't commit API keys, tokens, or anything private.
